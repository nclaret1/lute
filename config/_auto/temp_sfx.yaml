%YAML 1.3
---
# ========= HEADER (global) =========
title: "SFX: Pipeline - libpressio test"
experiment: "mfxx49820"
run: "16"
datasource: "exp=mfxx49820:run=16"
run_pad: "0023"
date: "2025/10/15"
lute_version: 0.1
task_timeout: 259200

work_dir: "/sdf/data/lcls/ds/mfx/mfxx49820/results/nclaret/lute_output/DemoLuteWorkflows/baseline_peakfinder8/r0016"
...
---
detector: "epix10k2M"
out_root:  "{{ work_dir }}"
pv_camera_length: "MFX:ROB:CONT:POS:Z"
event_receiver: "evr0"
geometry_file: "/sdf/data/lcls/ds/mfx/mfxx49820/results/nclaret/geom/r0016.geom"


# You must provide det_name and pv_camera_length
# pv_camera_length is the detector distance
# It can be a PV, or alternative a float for a fixed offset
# Example PVs are commented below, ask beamline staff for relevant PV
# Change outdir to an appropriate directory for CXI files.
FindPeaksSFX:
    outdir: "{{ out_root }}"
    det_name: "{{ detector }}"
    event_receiver: "{{ event_receiver }}"
    pv_camera_length: "{{ pv_camera_length }}"
    algorithm: "Peakfinder8"
    tag: "lyso"
    event_logic: false
    psana_mask: false
    mask_file: null
    min_peaks: 10
    max_peaks: 2048
    npix_min: 2
    npix_max: 30
    amax_thr: 40
    atot_thr: 180
    son_min: 10.0
    peak_rank: 3
    r0: 3.0
    dr: 2.0
    nsigm: 10.0

    #pv_camera_length: "MFX:DET:MMS:04.RBV" # MFX Rayonix
    #n_events: 20000
